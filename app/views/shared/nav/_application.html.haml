%ul.nav.navbar-nav
  - if @selected_company
    %li= link_to t('navigation.application.clients'), company_clients_path(@selected_company)
    %li= link_to t('navigation.application.product'), products_path
    %li= link_to t('navigation.application.categories'), categories_path